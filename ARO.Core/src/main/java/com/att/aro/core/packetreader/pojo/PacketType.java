package com.att.aro.core.packetreader.pojo;

public enum PacketType {
	/**
	 * The packet is an IPPacket. 
	 */
	IP_PACKET,
	/**
	 * The packet is an TCPPacket.
	 */
	TCP_PACKET,
	/**
	 * The packet is an UDPPacket. 
	 */
	UDP_PACKET
}
